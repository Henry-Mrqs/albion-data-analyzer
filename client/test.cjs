const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', err => console.log('PAGE ERROR:', err.message)); await page.goto('http://localhost:5173/crafting-calculator', {waitUntil: 'networkidle0'}).catch(e => console.log(e)); await page.type('.input-field', 't6'); await page.waitForSelector('.search-item'); await page.click('.search-item'); await new Promise(r => setTimeout(r, 2000)); await browser.close(); })();
